package constants

const (
	API = "/api"
	V1  = "/v1"
	// Auth
	AUTH          = "/auth"
	LOGIN         = "/login"
	SIGNUP        = "/signup"
	REFRESH_TOKEN = "/refresh-token"
	LOGOUT        = "/logout"
	CSRF_TOKEN    = "/csrf-token"
	// User
	USER = "/user"
	// Params
	ID_PARAM = ":id"
)
